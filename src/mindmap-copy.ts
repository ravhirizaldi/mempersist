/** Shared deterministic mindmap copy contract for server HTML and the bundled client. */
export interface MindmapClientCopy {
  accountLabel: string;
  conversationsLabel: string;
  emptyLabel: string;
  failedLabel: string;
  loadingLabel: string;
}
